<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bluedon.modules.student.dao.StudentDao">

    <select id="selectPageVo" resultType="com.bluedon.modules.student.entity.StudentVo">
        select str.id,str.age,str.password,str.user_name,str.email,
        cla.class_name,cla.class_no
        from t_student str left join t_classes cla on str.classes_id = cla.id
    </select>

    <select id="selectList" resultType="com.bluedon.modules.student.entity.StudentVo">
        select str.id,str.age,str.password,str.user_name,str.email,
        cla.class_name,cla.class_no
        from t_student str left join t_classes cla on str.classes_id = cla.id
    </select>

    <select id="selectListByParms" resultType="com.bluedon.modules.student.entity.StudentVo">
        select str.id,str.age,str.password,str.user_name,str.email,
        cla.class_name,cla.class_no
        from t_student str left join t_classes cla on str.classes_id = cla.id
        <where>
        1=1
        <if test="age !=null and age != ''">
            and str.age &gt; #{age}
        </if>
        <if test="userName !=null and userName != ''">
            and str.user_name = #{userName}
        </if>
        </where>
    </select>

</mapper>